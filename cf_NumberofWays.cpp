//http://www.practice.geeksforgeeks.org/problem-page.php?pid=1017
#include <iostream>
#include <string>
using namespace std;
#define ll long long

int main()
{
	int t;
	cin>>t;
	atring s;
	cin>>s;
	int n=strlen(s);
	int dp[n], last[n];
	ll sum[n];// sum[i]=dp[0]+dp[1]...+dp[i] and sum[n-1] will be the answer
	dp[0]=1;;// dp[i] stores no of distinct sub seq ending with a
	sum[0]=1;
	last[0]=
	return 0;
}